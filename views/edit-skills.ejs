<!DOCTYPE html>
<html>
<head>
  <title>Edit Skill</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

  <div class="container my-5" style="max-width: 600px;">
    <h2 class="text-center mb-4">Edit Skill</h2>

    <form action="/skills/edit/<%= skill._id %>" method="POST">
      <div class="mb-3">
        <label class="form-label">Skill Title</label>
        <input type="text" name="title" class="form-control" value="<%= skill.title %>" required>
      </div>
      <div class="mb-3">
        <label class="form-label">Skill Category</label>
        <input type="text" name="skillCategory" class="form-control" value="<%= skill.skillCategory || '' %>">
      </div>
      <button type="submit" class="btn btn-primary">Update Skill</button>
      <a href="/profile" class="btn btn-secondary ms-2">Cancel</a>
    </form>
  </div>

</body>
</html>
